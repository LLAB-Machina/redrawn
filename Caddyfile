{
    email admin@example.com
    auto_https disable_certs
}

http://localhost {
    encode gzip

    handle /api* {
        reverse_proxy api:8080
    }

    handle {
        reverse_proxy web:3000
    }
}

